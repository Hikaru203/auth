spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authdb_test
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    com.auth: DEBUG
